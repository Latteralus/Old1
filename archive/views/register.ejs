<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PharmaSim Register</title>
    <style>
        /* General Styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .header {
            background-color: #003366;
            color: white;
            text-align: center;
            padding: 1rem 0;
            font-size: 24px;
            font-weight: bold;
            width: 100%;
            position: absolute;
            top: 0;
        }
        .register-container {
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            width: 100%;
            max-width: 400px;
            margin-top: 6rem;
            text-align: center;
        }
        .register-container h1 {
            font-size: 22px;
            margin-bottom: 1.5rem;
            color: #003366;
        }
        .register-container form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .register-container input {
            width: 90%;
            margin-bottom: 1rem;
            padding: 0.75rem;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .register-container label {
            width: 90%;
            font-size: 0.9rem;
            color: #333;
            margin-bottom: 1rem;
            text-align: left;
        }
        .register-container label a {
            color: #003366;
            text-decoration: none;
        }
        .register-container label a:hover {
            text-decoration: underline;
        }
        .register-container button {
            width: 90%;
            background-color: #003366;
            color: white;
            border: none;
            padding: 0.75rem;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 1rem;
        }
        .register-container button:hover {
            background-color: #0056b3;
        }
        .home-container {
            margin-top: 1rem;
            width: 90%;
        }
        .home-button {
            display: block;
            width: 100%;
            background-color: #999999;
            color: white;
            border: none;
            padding: 0.75rem;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
        }
        .home-button:hover {
            background-color: #666666;
        }
        .error {
            color: red;
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <div class="header">PharmaSim</div>
    <div class="register-container">
        <h1>Register</h1>
        <form method="POST" action="/register">
            <input type="text" name="username" placeholder="Username" required>
            <input type="email" name="email" placeholder="Email Address" required>
            <input type="password" name="password" placeholder="Password" required>
            <input type="password" name="confirmPassword" placeholder="Confirm Password" required>
            <label>
                <input type="checkbox" name="terms" required>
                I agree to the <a href="/terms" target="_blank">terms and policies</a>.
            </label>
            <button type="submit">Register</button>
        </form>
        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <div class="home-container">
            <a href="/login" class="home-button">Home</a>
        </div>
    </div>
</body>
</html>
