version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build commands needed for static site"
    build:
      commands:
        - echo "Ensuring static site files are ready"
  artifacts:
    baseDirectory: ThePharmacy/main  # Correct directory for static site files
    files:
      - '**/*'  # Include all files and subdirectories
  cache:
    paths: []
