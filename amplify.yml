version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build commands needed for static site"
    build:
      commands:
        - echo "Ensuring static site files are ready"
  artifacts:
    baseDirectory: main  # Correct base directory for your files
    files:
      - '**/*'  # Include all files
  cache:
    paths: []
