version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build commands needed for static site"
    build:
      commands:
        - echo "Ensuring static site files are ready"
  artifacts:
    baseDirectory: main  # Correctly point to the main directory
    files:
      - '**/*'  # Include all files and subdirectories
  cache:
    paths: []
