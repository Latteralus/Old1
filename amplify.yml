version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build commands needed for static site"
    build:
      commands:
        - echo "Ensuring static site files are ready"
  artifacts:
    baseDirectory: .  # Output directory is the repository root
    files:
      - '**/*'  # Include all files and subdirectories
  cache:
    paths: []
