version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix ./client ci # Install frontend dependencies
    build:
      commands:
        - npm run build --prefix ./client # Build the React app
  artifacts:
    baseDirectory: client/build
    files:
      - '**/*'
  cache:
    paths:
      - client/node_modules/**/*